import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({

    palette: {
        primary: {
            main: '#069999',
        },
        secondary: {
            main: '#47BD77',
        },
        danger: {
            main: '#ff0000',
        }
    }
})

export default theme;