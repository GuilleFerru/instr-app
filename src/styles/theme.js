import { createTheme  } from '@material-ui/core/styles';

const theme = createTheme ({
    palette: {
        background: {
            default: "#ebeeef",
            paper: "#ffffff",
        },
        primary: {
            main: '#128726',
        },
        secondary: {
            main: '#47BD77',
        },
    }
})

export default theme;